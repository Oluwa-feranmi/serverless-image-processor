# serverless-image-processor
A fully serverless image processing pipeline built on AWS that automatically detects faces in uploaded images using Amazon Rekognition, stores metadata in DynamoDB, and sends notifications via SNS.
